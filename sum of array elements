#include <stdio.h>

int main() {
   int a,sum=0;
   scanf("%d", &a);
   
   int s[a];
   
   for(int i=0;i<a;i++){
       scanf("%d", &s[i]);
       sum+=s[i];
   }
   
   printf("%d", sum);
    return 0;
}

input:
5
1 2 3 4 5

Output:
15
